<h1>Pets</h1>
<table class="center">
  <tr>
    <th>Name</th>
    <th>Species</th>
    <th>Owner</th>
  </tr>
  <% @pets.each do |pet| %>
  <tr>

    <td><a href="/pets/<%= pet.id %>"><%= pet.name %></a></td>
    <td><%= pet.species %></td>
    <td><%= pet.owner_contact_info %></td>

  </tr>
  <% end %>
</table>
<p><a href="/pets/new">Add Pet</a></p>
